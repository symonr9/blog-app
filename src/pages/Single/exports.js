import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
    singleContainerDiv: {
        whiteSpace: 'pre-wrap',
        "& a": {
            textDecoration: 'none',
            color: 'black'
        }
    },
});